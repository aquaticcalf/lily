{
  "name": "db",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "dev": "vp run generate && turso dev --db-file local.db && drizzle-kit push",
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@libsql/client": "catalog:",
    "@paralleldrive/cuid2": "catalog:",
    "@t3-oss/env-core": "catalog:",
    "drizzle-orm": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "drizzle-kit": "catalog:"
  }
}
